'use strict';

const config = {
  // api_host: 'https://liquidpace-calendar-api.herokuapp.com/api',
  srv_host: 'localhost',
  srv_port: 3001,
  srv_prefix: '/srv/',
  clnt_host: 'localhost',
  clnt_port: 3000,
};

module.exports = config;
